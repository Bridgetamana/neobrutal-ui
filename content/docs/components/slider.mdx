---
title: Slider
description: An input where the user selects a value from within a given range.
---

<ComponentPreview 
code="import { Slider } from '@/components/ui/slider'\n\nexport function SliderDemo() {\n  return <Slider aria-label='Demo value' defaultValue={[50]} max={100} step={1} className='w-[60%]' />\n}">
  <div className="flex w-full justify-center">
    <Slider aria-label="Demo value" defaultValue={[50]} max={100} step={1} className="w-[60%]" />
  </div>
</ComponentPreview>

## Installation

<CodeBlock code="npx neobrutal add slider" language="bash" />

## Usage

<CodeBlock code="import { Slider } from '@/components/ui/slider'" />

<CodeBlock code="<Slider aria-label='Demo value' defaultValue={[50]} max={100} step={1} />" />


## API Reference

This component is built on top of [Base UI Slider](https://base-ui.com/react/components/slider). Refer to the Base UI documentation for the full list of available props.