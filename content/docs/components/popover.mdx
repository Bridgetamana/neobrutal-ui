---
title: Popover
description: Displays rich content in a portal, triggered by a button. Includes an arrow pointing to the trigger.
---

<ComponentPreview codeFile="popover/example-1.txt">
  <div className="flex w-full justify-center">
    <Popover>
        <PopoverTrigger render={<Button variant="noShadow" />}>
            Open Popover
        </PopoverTrigger>
        <PopoverContent>
            <div className="grid gap-4">
                <div className="space-y-2">
                    <h4 className="font-semibold leading-none">Dimensions</h4>
                    <p className="text-sm text-black">
                        Set the dimensions for the layer.
                    </p>
                </div>
            </div>
        </PopoverContent>
    </Popover>
  </div>
</ComponentPreview>

## Installation

<InstallTabs name="popover" />

## Usage

<CodeBlock code="import { Button } from '@/components/ui/button'\nimport { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover'" />

<CodeBlock code="<Popover>\n  <PopoverTrigger render={<Button variant='noShadow' />}>\n    Open Popover\n  </PopoverTrigger>\n  <PopoverContent>\n    <div className='grid gap-4'>\n      <div className='space-y-2'>\n        <h4 className='font-bold leading-none'>Dimensions</h4>\n        <p className='text-sm text-black'>\n          Set the dimensions for the layer.\n        </p>\n      </div>\n    </div>\n  </PopoverContent>\n</Popover>" />


## API Reference

This component is built on top of [Base UI Popover](https://base-ui.com/react/components/popover). Refer to the Base UI documentation for the full list of available props.